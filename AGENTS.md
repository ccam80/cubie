# CuBIE — repository root

CuBIE (CUDA Batch Integration Engine) is a Numba-CUDA JIT batch ODE/SDE solver.

Placeholder root doc pending a top-level redesign. The hierarchical, per-directory agent
documentation lives under `src/cubie/**/AGENTS.md` — each mirrored to a `CLAUDE.md` symlink so
tooling that reads `CLAUDE.md` loads it. Start at `src/cubie/AGENTS.md`.
